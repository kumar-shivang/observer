global:
  scrape_interval: 60s        # match the collector's COLLECT_INTERVAL
  evaluation_interval: 60s

scrape_configs:
  - job_name: observer
    static_configs:
      - targets: ["observer:8000"]
    metrics_path: /metrics
